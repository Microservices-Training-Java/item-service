spring:
  liquibase:
    change-log: classpath:/migration/master-changelog.xml
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:item}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:linh2002}
  application:
    name: item-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: item-service
  cloud:
    config:
      uri: http://localhost:8888
      label: label1
      profile: item-service
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka
server:
  port: 8081

application:
  i18n:
    resources:
      - classpath:item-service/message

